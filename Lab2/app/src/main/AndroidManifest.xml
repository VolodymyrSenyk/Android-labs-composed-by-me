<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.senyk.volodymyr.lab2">

    <permission
        android:name="com.senyk.volodymyr.lab2.permission.MY_PERMISSION"
        android:description="@string/my_permission_description"
        android:label="@string/my_permission_label"
        android:permissionGroup="android.permission.group.STORAGE"
        android:protectionLevel="dangerous" />

    <uses-permission android:name="com.senyk.volodymyr.lab2.permission.MY_PERMISSION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ProtectedActivity" />
    </application>

</manifest>
